console.log('We need to parse LangTagRecord array after the name records.');
// langTagRecord: length(uint16), langTagOffset(uint16)
// A language-tag record contains the length and offset for a language-tag string.
// Then how are these used? It seems they are referred to by `languageID`.
